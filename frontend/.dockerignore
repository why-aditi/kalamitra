node_modules
.next
.git
.gitignore

# Secrets must never enter an image layer. The previous list only excluded
# .env.local, so `COPY . .` shipped the real .env into the build.
.env
.env.*
!.env.example

Dockerfile
.dockerignore
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.tgz
*.tar.gz
.vscode
.idea
coverage
.cache
dist
README.md
