# Kalamitra backend dependencies.
#
# NOTE: stripe was pinned at 4.0.2 while the current major is 12.x. The webhook
# handling and error classes this codebase now uses need a modern release.
#
# Direct dependencies only - transitive packages resolve on their own.
# Removed as declared-but-never-imported: google-cloud-firestore,
# google-cloud-storage, sentry-sdk, python-jose, PyJWT, Jinja2, websockets,
# itsdangerous, ujson, orjson, fastapi-cli, fastapi-cloud-cli, rignore, tqdm,
# ecdsa, CacheControl.

fastapi==0.109.1
uvicorn==0.27.0
starlette==0.35.1
python-multipart==0.0.9
python-dotenv==1.0.0

pydantic==2.11.7
pydantic-settings==2.10.1
email-validator==2.1.0.post1

motor==3.7.1
pymongo==4.9.0
dnspython==2.7.0

firebase-admin==6.4.0
google-generativeai==0.8.5

stripe==12.5.1

pillow==11.3.0
# httpx must stay < 0.28: httpx 0.28 dropped the `app=` constructor argument
# that starlette 0.35.1's TestClient still passes, which breaks every test.
httpx==0.27.2
requests==2.32.4

# Tests (B5: payment-path coverage). Version verified against this suite.
pytest==9.1.1
